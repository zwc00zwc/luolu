@model Luolu.Models.FreightTemplateInfo
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index1</title>
</head>
<body>
    <div>
        <div>这是index1的页面</div>
        <ul>
            @*@foreach (var item in Model)
            {
                <li>@item.name</li>
            }*@
            @ViewBag.show
        </ul>
    </div>
</body>
</html>
